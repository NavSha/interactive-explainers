# Entities

Named things referenced in articles: models, tools, protocols, patterns, orgs.

Create an entity page when:
- A named thing appears in 2+ articles, OR
- It's worth a canonical short definition a reader can click through to.

Candidate entities (populate as the site grows):

- `mcp` — Model Context Protocol (agents, grounding).
- `claude` — Anthropic's model family.
- `react-pattern` — Reasoning + Acting. (could be theme or entity — pick one)
- `vector-db` — class of tool referenced in grounding, context-windows.
- `evals-harness` — pattern covered in evaluation.

See [`../SCHEMA.md`](../SCHEMA.md) for the entity frontmatter and section layout.
